APP_NAME="Internships"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

API_GEOCODE_URL="https://api.mapbox.com/geocoding/v5/mapbox.places/"
API_GEOCODE_TOKEN=""

LOG_CHANNEL=stack
LOG_LEVEL=debug

CURRENT_UID=1000

DB_CONNECTION=mysql
DB_HOST=intern-mysql
DB_PORT=3306
DB_DATABASE=intern
DB_USERNAME=intern
DB_PASSWORD=password
DB_ROOT_PASSWORD=example

DB_SEED_ADMIN_EMAIL=admin@example.com
DB_SEED_ADMIN_PASSWORD=password

DOCKER_DEV_DB_EXTERNAL_PORT=5027
DOCKER_DEV_DB_DATABASE=${DB_DATABASE}
DOCKER_DEV_DB_USERNAME=${DB_USERNAME}
DOCKER_DEV_DB_PASSWORD=${DB_PASSWORD}
DOCKER_DEV_DB_ROOT_PASSWORD=${DB_ROOT_PASSWORD}

EXTERNAL_WEBSERVER_PORT=80
EXTERNAL_NODE_PORT=5857

REDIS_PORT=6379
REDIS_HOST=intern-redis

DOCKER_INSTALL_XDEBUG=false

XDG_CONFIG_HOME=/tmp

BROADCAST_DRIVER=log
CACHE_DRIVER=file
QUEUE_CONNECTION=redis
SESSION_DRIVER=redis
SESSION_LIFETIME=120
FILESYSTEM_DISK=local

MAILHOG_PORT=1025
MAILHOG_DASHBOARD_PORT=8025

MAIL_MAILER=smtp
MAIL_HOST=mailhog
MAIL_PORT=${MAILHOG_PORT}
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=hello@example.com
MAIL_FROM_NAME="${APP_NAME}"

VITE_HOST=0.0.0.0
VITE_HMR_HOST=localhost
VITE_USE_POLLING=true
VITE_PORT=${EXTERNAL_NODE_PORT}
VITE_PUBLIC_DIRECTORY_PATH=../public
VITE_MAPLIBRE_TOKEN=""
